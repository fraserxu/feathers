// Utilities
// ---------

// Libraries.
var cookie = require('component-cookie');
var query = require('component-query-string');

// Load everything that's needed for the application.
function loadApplication(callback) {
    callback(null, {});
};
